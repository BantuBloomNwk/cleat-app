.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool
# Every keypair a script persists. One pattern rather than a list that has to
# be remembered each time a script gains an owner.
scripts/.*.json
app/node_modules
app/dist

# Local Netlify folder
.netlify
app/.netlify
.env

# The submission write up. Deadlines, which bounty tracks were entered and
# which were passed on and why, and notes for whoever shoots the video. That
# is working material for one week and it is nobody else's business, so it
# stays on this machine.
SUBMISSION.md

# A compiled program kept so a bad deploy can be undone. Useful locally,
# a megabyte of binary and nothing else to a reader.
.rollback/
