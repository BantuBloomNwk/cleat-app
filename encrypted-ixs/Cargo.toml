[package]
name = "encrypted-ixs"
version = "0.1.0"
edition = "2021"

[dependencies]
# Exact, like every other arcium crate here. A caret on arcis lets cargo
# pull a newer compiler than the client and the on chain program were
# built against, and the three have to agree on the circuit format.
arcis = { workspace = true }
